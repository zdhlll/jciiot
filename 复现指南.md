# 复现指南

本指南用于从本仓库干净克隆并复现五关满分成绩（100/100，2026-08-15 证据）。

## 1. 环境要求

- **操作系统**：Linux（成绩在 Ubuntu 22.04 / 内核 6.8 上取得；官方推荐 Windows 亦可，但本方案未在 Windows 复测）
- **Python**：3.11（Miniconda/venv 均可）
- **Git LFS**：必须安装（官方 `robosuite/robosuite/model_epoch_150.pth` 等大文件以 LFS 指针形式入库；**注意：本方案抓取完全脚本化，不加载该模型，不拉取 LFS 对象也能复现成绩**）
- 依赖清单见 `JCIIOT/requirements.txt`（含 mujoco、robosuite、streamlit、numpy 等）与 `pyproject.toml`

## 2. 安装

```bash
# 1) 克隆（评审锁定版本）
git clone https://github.com/zdhlll/jciiot.git
cd jciiot
git checkout submission-final-100     # 或直接使用 main 分支 HEAD

# 2) （可选）拉取官方 LFS 大文件 —— 脚本抓取路线不需要，仅完整环境校验用
# git lfs pull

# 3) Python 环境
conda create -n jciiot python=3.11 -y
conda activate jciiot
pip install -r JCIIOT/requirements.txt
pip install -e ./JCIIOT/robosuite
pip install -e ./JCIIOT
```

## 3. 运行

### 3.1 LLM 服务配置（SOP 生成与任务分解用）

`JCIIOT/knowledge/robot_params.json` 的 `llm` 段配置文本模型与视觉模型（ollama 本地服务或 OpenAI 兼容 API）。API 密钥一律通过环境变量传入，**不要**写入任何提交文件。SOP 知识库已随仓库提交为团队生成版，默认无需重新生成；如需重新生成：

```bash
# 离线生成（从官方 docx，输出覆盖 JCIIOT/knowledge/sop*.md）
python JCIIOT/sop_autogen_tool/generate_sops.py
```

运行态也会由 `GenerateSOPSkill`（`src/robot_agent/skills/sop_generator.py`）按需生成。

### 3.2 执行任务（官方入口）

```bash
cd JCIIOT
python -m streamlit run app.py
```

在界面中开启 Knowledge Base 注入，依次对 L1–L5 点击 Execute。系统自动运行任务、逐帧记录轨迹并给出官方评分。执行链路：`move → pick_up → move → place_down`（L5 为三物体循环），全程物理仿真，无瞬移。

### 3.3 无模型依赖说明

`skills/pick_up.py` 中 `_GRASP_MODE = "scripted"`，抓取由相位表脚本直接驱动，**不加载任何 `.pth` checkpoint**（`robot_params.json` 的 `grasp_policy.checkpoint_path` 仅在被关闭的 "bc" 模式下才生效）。仓库不包含团队模型文件，`team_submission/models/` 亦无内容。

## 4. 验证

```bash
# 提交自检：五关证据齐全 + 满分 + 轨迹 schema + 受保护边界审计
python JCIIOT/team_submission/audits/verify_final_submission.py

# 受保护边界复核（团队改动相对官方内容基线的完整 diff）
git diff 0dcdddf18a9e694569aa1433cdfc04eb097fed78..HEAD --stat
```

- 证据位置：`JCIIOT/team_submission/evidence/L1~L5/`，每关 `trajectory.json`（官方 `trajectory_template.json` schema，可回放）+ `score.json`（官方评分输出）+ `result.json`（任务 manifest）。
- 回放：官方 `robosuite_backend.replay_trajectory(json_path)` 可逐帧重放轨迹；或直接在 app.py 中查看对应运行的评分明细（L5 为 3 物体 × 2 项打分）。

## 5. 提交证据的复现边界

- 五关满分证据取自 2026-08-15 18:41–18:48 的连续运行，`score.json` 内 `score_rule_version = grasp_success_gate_l5_multi_v2`，评分由官方 `app.py` 内置 `_score_steps()` 自动给出，未作任何改动。
- 建议评审在干净环境按本指南依次复测 L1–L5；单关复测通过的关键前提是第 3.1 节的 LLM 服务可用（任务分解/知识库注入需要），机械臂执行本身不依赖任何模型权重。
