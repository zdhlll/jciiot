# JCIIOT 2026 工业具身智能挑战赛技术报告

## 1. Technology Description

### 1.1 总体架构

方案构建在官方 VLA 具身智能平台（MuJoCo + robosuite + robomimic，Tiago 移动双臂机器人，FactorySorting 场景）之上，执行链路为官方 `app.py` 入口 → LLM 任务分解 → 技能执行 → 逐帧轨迹记录与官方计分。团队的全部改动**严格限定在官方允许修改的区域**（`src/robot_agent/skills/`、`workflows/`、`knowledge/robot_params.json` 及官方 SOP 规则允许覆盖的 `knowledge/*.md`），官方锁定区（`core/`、`environments/`、`app.py`、`task_config.json`、`robosuite/`）与官方内容基线逐字节一致（审计报告见《提交合规说明.md》）。

### 1.2 SOP 自动编译：文档—任务—地图三重约束

按官方要求，团队用自研代码从 `sop+prompt/` 的 Word 文档自动生成知识库 `.md` 文件（覆盖官方参考版，官方规则明确要求不得直接复用参考版）。生成链路刻意分离两个模型角色（`src/robot_agent/skills/sop_generator.py`，离线版 `JCIIOT/sop_autogen_tool/generate_sops.py`）：

- **VLM**：读 SOP 文档中的示意图、高亮站点标注、物料外观；
- **文本 LLM**：抽取任务语义与操作步骤；
- **对账层**：把模型输出与 `knowledge/task_config.json`（锁定区，只读）、`generated_maps/*_semantic_map.json`、场景源码中的真实站点 ID / 坐标 / 模拟器物体名逐一核对，消除幻觉站点与坐标漂移。

生成结果直接驱动后续执行——"Pick Station 2 / Place Station 3" 等自然语言站点在知识库中映射为 `input_5 / output_4` 等模拟器端口。

### 1.3 单一审计工作流与技能路由

`skills/library.py` 在竞赛环境中把 LLM 计划中出现的全部技能名（`move` / `pick_up` / `place_down` / `analyze_supply`）统一别名路由到**同一条经过审计的确定性搬运工作流**（`analyze_supply.py` 的 `AnalyzeSupplySkill`），并用结果缓存保证计划中同名多步**幂等执行**（不重复跑、不漏跑）。路由在技能库层实现，**不修改官方 `core/agent.py`**。

工作流骨架固定为 `move → pick_up → move → place_down`（多物体关卡按物体循环），每步的输入由编排层注入结构化的 `target / object_name / grasp_pose`，不依赖 LLM 自由输出。

### 1.4 任务表单一数据源与关卡适配

任务定义（源站 / 目标站 / 目标物体）**只读官方 `knowledge/task_config.json`**（`_task_spec`），代码中不硬编码关卡坐标。各关适配要点：

- **L1–L4**：候选物体任取其一（`object[:1]`）；
- **L5**：三个白色 tote 必须按 **front → center → back** 顺序抓取——先取中间会使两臂包络在后拉时扫走后面箱子的支撑，导致其翻倒；
- 抓取站位**一律从运行中的 MuJoCo 几何现算**（`_live_standoff_pose`），不读地图坐标——L2 语义图 approach 点是两个并排 tote 的中点，与真实停靠位不符，硬读必错。

### 1.5 导航：A* 与物理安全策略

`skills/move.py` 在语义占用栅格上执行 A*（`min_spacing=0.35m`），并有三层兜底：

1. **最近可达回退**：目标被占用时，栈式遍历起点连通分量、取离目标最近的格子重新规划；
2. **尾段直达**：语义网格可能把裁判验证过的抓取点标成占用，主干路径保持网格安全后，仅把最后一段短尾接到精确目标；
3. **分段 waypoint 物理行驶**：全程真实底盘驱动，无瞬移。

关卡级安全通道（`analyze_supply.py`）：

- **出生点北侧出站通道**（L3/L4/L5）：出生姿态双臂西伸于工作高度，直接 A* 斜线会扫过 line 6 设备模块——先驶向几何导出的北侧出站 waypoint 再继续；西端 waypoint 锚定在走廊地板纬度（由同一条 A* 路径的最低纬度现算），机器块压窄通道时不再折返爬升；
- **货架离场走廊**（L2/L3）：携带箱悬在底盘外侧约 0.95m，先沿几何导出的走廊拉出货架再进过道，避免箱体刮擦；
- **L5 北走廊回程**：空车回程走北侧过道中线（由目标桌南沿与 input_1 北沿现算），替代旧南侧绕行路线，每次回程省约 34m。

**转向合入导航**（`skills/rotating_approach.py` + `move.py`）：底盘为全向直驱，旋转与平移可同帧叠加。A* 路径末段沿网格安全腿边开边转到目标 yaw（固定转速 7.2°/步，与官方原地转向同档），到点后原地转向只剩零角度残量。四层保护：腿长容量检查、携箱扫过带预检（方向感知，双向尝试）、到点 yaw 容差兜底、逐帧碰撞监控，任一不满足即回落已验证的原地转向。

### 1.6 抓取：阶段表脚本抓取（零 BC 模型）

`skills/pick_up.py` 中 `_GRASP_MODE = "scripted"`，**完全不使用任何 BC checkpoint**：

- 相位表驱动：`diagonal_approach`（80 步斜线插值逼近）→ `vertical_descent`（70 步下降），每相位先按步数插值、再以有界闭环收敛，**逐阶段报告成败**，失败可定位到具体阶段；
- **夹点模板旋转**：把官方抓取 site 对绕物体中心旋转到正对底盘侧再降到夹持深度（对 L2 tote 这类模板朝向与底盘侧不一致的物体，回退到朝向机器人的碰撞墙几何：进深 = 墙在近轴投影，半跨按模板/物体半宽比例缩放）；
- 抬升保护：起点过低时先补 20 步纯抬升，防止从低姿态斜插扫到桌面/货架；抓取后竖直抬升 200mm（`robot_params.json` 的 `lift.lift_height`），携带箱底全程高于邻箱顶，五关邻物零擦推；
- 双机械臂夹爪中心闭环对齐、抓取全程姿态锁定（`_PostureLock`）；
- **融合回缩**（`skills/fused_retract.py`）：抓取后手臂收回到 ready 姿态不再一次性直设，而是挂在 pending 姿态上，由离站腿前 12 步边行驶边逐帧插值（约 4°/步/关节），零原地等待、轨迹无关节瞬移——驱动段全部实现于允许修改的 `skills/`，官方 `environments/` 驱动层逐字节未动。

### 1.7 放置：物理放置与多物体槽位

- 常规输出端口走官方物理放置接口；
- 地图里有、场景端口表里没有的目标（L3–L5 的 `output_5/6`）走**扩展重力释放**：放置前转向已由移动层沿网格安全腿合并完成（携物转向期间锁定上肢姿态、只允许夹爪自由度，防止箱子/手臂扫落邻物），到位后只剩零角度残量原地转向 → 同步附着 → 松爪 → 重力自然落桌；
- **L5 三箱槽位**：沿桌面长轴以 0.59m 中心距排开（0.48m 宽箱不重叠），按右→左顺序填充，携带箱始终背向已上桌的箱子。

### 1.8 第三方库与依赖

官方仓库自带：robosuite（MuJoCo 仿真环境）、robomimic（未使用其推理）、streamlit（前端）、numpy 等（见 `JCIIOT/requirements.txt`、`pyproject.toml`）。团队未引入任何新第三方库，未替换官方抓取模型，未新增 LFS 对象。

## 2. Results & Analysis

### 2.1 总成绩

2026-08-16 20:18–20:26 连续运行五关，**总分 100/100，全程无碰撞扣分**。评分全部由官方 `app.py` 内置 `_score_steps()` 依据执行轨迹 JSON 自动计算（`score_rule_version: grasp_success_gate_l5_multi_v2`）。

### 2.2 逐关成绩

| 关卡 | 场景 | 任务（源 → 目标 / 物体） | 得分 | 用时 | 轨迹帧数 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| L1 | FactorySorting1 | input_5 → output_4 / line_5_container | 10/10 | 67.7 s | 480 |
| L2 | FactorySorting3 | input_6 → output_4 / green_tote | 15/15 | 67.7 s | 463 |
| L3 | FactorySorting5 | aux_input_1 → output_5 / blue_tote | 20/20 | 68.2 s | 508 |
| L4 | FactorySorting7 | input_2 → output_5 / blue_container | 25/25 | 103.3 s | 701 |
| L5 | FactorySorting9 | input_1 → aux_output_1 / white_tote ×3 | 30/30 | 241.2 s | 1579 |

### 2.3 分析

- **L1/L2 单物体短途**：一次抓取一次放置，时间主要由导航与抓取阶段插值步数决定（≈68s），两关难度差异体现在站位几何（L2 双 tote 并排、模板朝向不一致，靠碰撞几何兜底解决）。
- **L3 辅助输入**：`aux_input_1` 南侧接近 + 货架离场走廊，额外安全绕行使时间升至 68.2s，但仍无碰撞。
- **L4 大箱体**：blue_container 尺寸更大，导航净空更紧，103.3s。
- **L5 三物体**：三次完整往返（含两次北走廊空车回程）主导时长 241.2s；三箱按 front→center→back 顺序抓取、0.59m 槽位排布，实现三物体零翻倒零碰撞满分。
- 全程五个轨迹中 `has_collision` 未触发扣分，说明碰撞规避策略（出站通道、离场走廊、合并转向、姿态锁定）有效。
- 轨迹帧数已达当前代码的机械下限附近：导航帧 ≈ A* 网格最优路径步数（如 L1 里程 35.2m ÷ 0.1m/步 ≈ 352 步，占 480 总帧的主体），静止帧集中于抓取/放置相位（经验安全值 + 夹爪物理限幅）。

### 2.4 证据

五关满分轨迹 / 评分 / 结果文件：`JCIIOT/team_submission/evidence/L1~L5/`（trajectory.json 符合官方 `competition description/trajectory_template.json` schema，可由官方 `replay_trajectory` 回放）。

## 3. Compliance

- 官方锁定区与基线提交逐字节一致，团队改动全部落在允许区域（详见《提交合规说明.md》与 `team_submission/audits/official_boundary_audit.json`）；
- SOP 知识库为团队代码自动生成（官方规则要求，见 Contestant Manual "SOP Knowledge-Base Rules"），生成代码随仓库提交供评委审查；
- 未使用、未替换任何 BC checkpoint；官方 `model_epoch_150.pth` 保持官方 LFS 指针原样。

## 4. Limitations

- 方案面向竞赛五关的 FactorySorting 系场景做了关卡级几何策略（出站通道、离场走廊、L5 顺序），未验证跨工厂布局的泛化能力；
- 抓取相位表步数与导航间距等参数经实验标定，极端扰动下依赖逐阶段失败报告与重试机制，未做大规模随机化鲁棒性测试；
- 运行依赖本地 LLM（SOP 生成/任务分解），复现环境需配置可用的文本/VLM 服务。
