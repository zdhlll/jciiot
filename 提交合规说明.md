# 竞赛提交合规说明

本文件按官方《Contestant Manual》（仓库 README）、FAQ 与主办方培训要求，逐项说明本提交的合规性。全部结论可自动复核：`python JCIIOT/team_submission/audits/verify_final_submission.py`。

## 1. 结论

**合规。** 团队改动全部落在官方允许修改区域；官方禁止修改区域与官方内容基线逐字节一致；SOP 知识库为团队代码自动生成（官方规则要求）；未使用、未替换任何抓取模型。

## 2. 官方规则逐项对照

### 2.1 允许修改区域（官方原文）

> ✅ **Allowed to modify**: `src/robot_agent/skills/`, `src/robot_agent/workflows/`, `knowledge/robot_params.json`

团队改动：
- `src/robot_agent/skills/`：`analyze_supply.py`、`move.py`、`pick_up.py`、`place_down.py`、`library.py` 修改；`sop_generator.py` 新增（SOP 生成技能）。✅
- `src/robot_agent/workflows/`：官方文件保持未修改（本地实验脚本 `collect_factory_sorting.py` 已排除在提交之外）。✅
- `knowledge/robot_params.json`：导航/抓取/放置参数调整。✅

### 2.2 禁止修改区域（官方原文）

> ❌ **Forbidden to modify**: `src/robot_agent/core/`, `src/robot_agent/environments/`, `app.py`, `knowledge/task_config.json`

- 全部与官方内容基线**逐字节一致**（见第 3 节审计）。✅
- 另按主办方培训要求，`robosuite/` 亦保持未修改（含官方 LFS 模型 `model_epoch_150.pth` 指针原样、场景环境文件零改动）。✅

### 2.3 SOP 知识库规则（官方原文）

> Contestants **must write their own code** … so that the large model can read the Word files above and automatically generate `.md` knowledge-base files that are functionally equivalent to the manually rewritten versions.
> **Directly reusing the existing `sop*.md` files … will lead to point deductions.**
> Contestants may place the newly generated `.md` files in the `knowledge/` directory by overwriting old files or creating new ones. However, the code used for generation must be kept in `skills/` or `workflows/` for judges to review.

本提交对应：
- `knowledge/sop1~5.md`、`sop_main.md` 为团队代码从 `sop+prompt/*.docx` 自动生成的版本（文件头 `AI-GENERATED FROM DOCX`），**未直接复用官方参考版**。✅
- 生成代码保留在仓库：`src/robot_agent/skills/sop_generator.py`（运行态技能）与 `JCIIOT/sop_autogen_tool/generate_sops.py`（离线生成器，含"不读取官方 sop*.md、由本地模板复现形状、可审计"的设计说明）。✅
- `knowledge/task_config.json`（官方锁定文件）在生成链路中**只读**，未修改。✅

### 2.4 抓取模型

官方允许"自训练 robomimic checkpoint 并替换默认抓取模型"。本方案选择**不替换**：`_GRASP_MODE = "scripted"` 全程脚本抓取，官方 `model_epoch_150.pth` 保持官方 LFS 指针不变，仓库无任何团队模型文件。✅

## 3. 受保护边界审计

- **基线**：提交 `0dcdddf`（本 fork 的官方内容基线）。经 GitHub API 树比对，该提交与官方仓库 `JCIIOT2026/JCIIOT2026` master（`80d8f92`，2026-08-14 最新）**仅 README.md 不同**，`JCIIOT/` 及受保护子树完全一致——即团队改动全部集中在基线之上，审计基准可靠。
- **方法**：`git ls-tree` 比对基线与本提交在受保护路径（`JCIIOT/app.py`、`JCIIOT/knowledge/task_config.json`、`JCIIOT/src/robot_agent/core/`、`JCIIOT/src/robot_agent/environments/`、`JCIIOT/robosuite/`）下的全部对象哈希；并对 `git diff 0dcdddf..HEAD` 的全部改动路径做允许区域白名单检查。
- **结果**（由 `verify_final_submission.py` 自动生成，落盘 `JCIIOT/team_submission/audits/official_boundary_audit.json` / `.md`）：
  - 受保护文件检查：**修改 0 / 新增 0 / 缺失 0**
  - 越界改动路径：**0**
  - 团队改动分布：skills/、knowledge/（SOP 与参数）、team_submission/、sop_autogen_tool/、`.gitignore`、根目录 5 份提交文档

## 4. 正式客观成绩

| 关卡 | 得分 | 证据 |
| ---- | ---- | ---- |
| L1 | 10/10 | `team_submission/evidence/L1/` |
| L2 | 15/15 | `team_submission/evidence/L2/` |
| L3 | 20/20 | `team_submission/evidence/L3/` |
| L4 | 25/25 | `team_submission/evidence/L4/` |
| L5 | 30/30 | `team_submission/evidence/L5/` |

- 运行时间：2026-08-15 18:41–18:48（连续五关）；评分由官方 `app.py` 内置 `_score_steps()` 依据轨迹 JSON 自动计算，`score_rule_version = grasp_success_gate_l5_multi_v2`，未修改任何评分逻辑。
- 提交中不含：运行录屏目录 `recordings/`、训练中间模型、私有演示数据、本地 API 凭据（`.local/`）、场景修改实验（`new/`）——以上全部由 `.gitignore` 排除。

## 5. 打包前核对结果

- [x] 受保护边界审计通过（0 违规）
- [x] 五关满分 score/result/trajectory 证据纳入提交
- [x] SOP 生成代码保留在 skills/ 与 sop_autogen_tool/
- [x] 无团队模型文件、无新增 LFS 对象
- [x] 无凭据、无私有数据入库
- [x] 评审锁定标签 `submission-final-100`
