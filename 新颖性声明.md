# 新颖性声明

## 声明摘要

本方案的新颖性不在提出新的网络结构或新的大模型理论，而在于**一套面向竞赛规则与物理仿真约束的确定性工程方法**：以官方 SOP Word 文档、锁定区任务配置与语义地图三重约束自动编译知识库，以单一审计工作流收敛 LLM 计划的不确定性，以**完全脚本化的阶段表抓取替代 BC 行为克隆模型**（零模型依赖），并把合规性做进 git 历史本身（团队改动 = 相对官方内容基线的一个可审计 delta）。全部主张均可通过仓库代码、五关满分轨迹与自检脚本复核。

## 1. 文档—任务—地图三重约束的 SOP 编译

官方要求参赛者用自写代码把 Word SOP 自动生成为知识库 `.md` 文件（不得直接复用参考版）。本方案将生成链路设计为**双模型角色 + 硬约束对账**：

- VLM 负责示意图、高亮标注与物料外观；文本 LLM 负责任务语义与步骤；
- 两者输出与锁定区 `task_config.json`、语义地图 `*_semantic_map.json`、场景源码中的站点 ID/坐标/物体名**逐一核对**，模型输出的"站点别名"被强制收敛到模拟器真实端口（如 Pick Station 2 → `input_5`）。

新颖点：不把 SOP 生成当作一次性 LLM 翻译，而是**编译**——以不可修改的官方数据为类型系统，消除幻觉坐标与站点错配。

## 2. 单一审计工作流 + 技能别名路由

LLM 任务分解天然存在措辞不确定性（同一任务可能计划出不同技能序列）。本方案在**技能库层**（`skills/library.py`）把竞赛环境中所有相关技能名统一别名到同一条确定性搬运工作流，并以结果缓存实现**幂等执行**：计划中的同名多步既不重复执行也不遗漏。官方 `core/agent.py` 零修改。

新颖点：不抑制 LLM 的规划自由度，而是把执行收敛到唯一经过审计的物理流程，使"计划不确定、执行确定"。

## 3. 完全脚本化的分阶段抓取（零 BC 模型依赖）

官方路线建议"自训练 robomimic BC 模型替换默认抓取模型"；本方案在 `_GRASP_MODE = "scripted"` 下**彻底绕开模型**，由相位表脚本直接驱动 MuJoCo 物理仿真完成抓取：

- 相位表（斜线逼近 80 步 + 垂直下降 70 步）按步数插值 + 有界闭环收敛，**逐阶段成败报告**——失败可定位到具体阶段而非"模型输出不佳"；
- 夹点模板绕物体中心旋转到正对底盘侧；模板朝向不可用时回退到碰撞墙几何（进深/半跨按比例推导）；
- 抓取站位从**运行中几何现算**（L2 语义图 approach 点为两个并排 tote 中点、与真实停靠位不符，读地图必错）。

新颖点与收益：(a) 抓取全程**可解释、可逐阶段调试**，对比黑盒 BC 推理；(b) 仓库**不包含任何团队模型文件**，不需要 Git LFS 大对象、不存在 checkpoint 与证据不匹配问题，复现门槛显著降低；(c) 完全物理执行，无瞬移/贴附捷径。

## 4. 携物风险感知的物流规划

携带物悬在底盘外侧约 0.95m，底盘足迹安全 ≠ 箱体安全。方案把"箱体净空"显式建模进导航决策：

- **货架离场走廊**（L3）：几何导出的出站走廊，先向东拉出货架再进过道；
- **出生点北侧出站通道**（L3/L5/L7）：避免出生姿态双臂西伸时 A* 斜线扫过 line 6 设备；
- **L5 空车回程北走廊中线**：比旧南绕路线每次回程省约 34m；
- **L5 抓取顺序 front→center→back**：先取中间箱会把后箱支撑扫走致其翻倒；
- **多物体放置槽位**：0.59m 中心距沿桌轴排布 + 右→左填充顺序，携带箱始终背向已上桌箱体；
- **放置期上肢姿态锁定**：转向/松爪期间锁死非夹爪自由度，防箱体扫落邻物。

新颖点：把"携带物外形"作为一级对象参与路径与顺序规划，而不是仅做底盘避障。

## 5. 提交可审计性

团队提交基于与官方仓库逐字节一致的基线提交 `0dcdddf`，全部改动集中在基线之上的少量提交里：评审可用 `git diff 0dcdddf..HEAD` 直接查看每行改动，配合 `team_submission/audits/verify_final_submission.py` 自动断言（禁区零修改、五关证据满分、轨迹 schema 合规、SOP 生成代码保留）。审计结果落盘为 `official_boundary_audit.json`。

## 6. 定量证据

| 指标 | 结果 |
| ---- | ---- |
| 总分 | 100/100（L1–L5 满分） |
| 碰撞扣分 | 0（五关轨迹均无碰撞） |
| 各关用时 | 74.6 / 73.0 / 80.4 / 111.3 / 257.1 s（合计 596.4s） |
| 团队模型文件 | 0 个（无 BC checkpoint） |
| 禁区改动 | 0 个文件（与官方基线逐字节一致） |
| 官方 SOP 直接复用 | 否（知识库为团队代码自动生成） |

## 7. 原创范围与局限

- 不宣称新网络结构、新训练方法或大模型贡献；A*、MuJoCo/robosuite 物理接口、官方评分与轨迹 schema 均直接复用官方提供物；
- 关卡级几何策略（出站通道、离场走廊、L5 顺序）针对竞赛五关布局设计，未验证跨布局泛化；
- 抓取相位表与导航参数为实验标定值，极端扰动下的鲁棒性依赖逐阶段失败重试，未做大规模随机化测试。

## 8. 可复核入口

- 代码：`JCIIOT/src/robot_agent/skills/`（analyze_supply / move / pick_up / place_down / library / sop_generator）
- 证据：`JCIIOT/team_submission/evidence/L1~L5/`
- 自检：`python JCIIOT/team_submission/audits/verify_final_submission.py`
- 审计报告：`JCIIOT/team_submission/audits/official_boundary_audit.md`
